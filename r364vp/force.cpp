#include <iostream>
#include <cstdio>
#include <algorithm>
#define rep(i,a,b) for(int i = a; i <= b; i++)
using namespace std;
const int N = 1010;
int a[N], b[N], x[N], y[N], w[N];
int main() {
	int n, m; cin >>n>>m;
	int s, t; cin >>s>>t;
	rep(i,1,m) cin>>x[i]>>y[i]>>w[i];
	rep(i,1,m) rep(j,1,m) {
		
	}
}